#!/bin/bash
# Store the current owner of the file in a variable
current_owner=$(stat -c '%U' hello)

# Check if the current owner is guillaume
if [ "$current_owner" = "guillaume" ]; then
    # Change the owner to betty
    chown betty hello
fi
